# 017 — CourseGPT completion integration

User authorization: complete development gaps identified in the 2026-09-06 mail/repository comparison. Instructor email remains product-reference material.

## Acceptance
1. Preserve the 013→014→015 product journeys and 016 tooling; integrate the existing PR23/25 changes without losing source ancestry or immutable evidence.
2. Student practice obeys instructor hint limits, including zero; finished results hide during a newly active exam; a session whose visible question set becomes empty can still be finished.
3. Acceptance packets hash the exact exported bytes and bind real samples to the candidate; scoring never overwrites existing human decisions or output files.
4. Local API documentation assets are included in the runtime image, and OpenAPI content checks respect the administrator access boundary.
5. Fresh isolated API, web, browser, migration/RLS and contract/document gates are recorded against the final integration content.
6. Main-targeted aggregate AI lineage covers the complete change. Existing dossiers remain immutable. Real model, human acceptance, staging and production are never inferred from local checks.

## Non-goals
No new autonomous agent, code execution service, framework redesign, global permission change, main merge or production deployment. No invented human labels or approval. No unlimited provider calls.

## Risk
R3: assessment/exam integrity, instructor policy, protected OpenAPI access and evaluation evidence integrity. Keep feature flags default-off and existing fail-closed source/role boundaries.
